"""Gates: whole-pipeline checks that the merge did not change the numbers.

Unit tests assert that a function does what it says. These assert that the
merged engine reproduces what the two pre-merge pipelines actually published,
from the same inputs, which is the only evidence that thirty modules were ported
rather than rewritten.
"""
